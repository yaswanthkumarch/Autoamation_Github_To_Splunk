name: Fake CI Echo Workflow

on:
  workflow_dispatch:  # you can run it manually

jobs:
  mock-job:
    runs-on: ubuntu-latest
    steps:
      - name: Start Agent
        run: echo "ð¢ Starting agent on ubuntu-latest"

      - name: Checkout Code
        run: echo "ð¦ Checking out code from repository"

      - name: Set Up Node.js
        run: echo "ð ï¸ Setting up Node.js environment"

      - name: Install Dependencies
        run: echo "Installing dependencies (npm install)"

      - name: Run Tests
        run: echo "ð§ª Running tests"

      - name: Build Application
        run: echo "ðï¸ Building application"

      - name: Deploy to Staging
        run: echo "ð Deploying to staging environment"

      - name: Done
        run: echo "â Workflow completed successfully"
      - name: Done
        run: echo "â Workflow completed successfully and flow created successfully "
